# site_i <-unique(data$cancer_group)[!is.na(unique(data$cancer_group))]
# pub_year_i <- seq(min(data$pub_year),max(data$pub_year))
# study_start_i <- seq(min(data$study_start,na.rm=T),max(data$study_start,na.rm=T))
# study_end_i <- seq(min(data$study_end,na.rm=T),max(data$study_end,na.rm=T))
# no_pat_i <- levels(data$no_pat)
# poc_i <- unique(data$poc)[!is.na(unique(data$poc))]
# source_i <- unique(data$source)[!is.na(unique(data$source))]
# country_i <- sort(unique(data$country)[!is.na(unique(data$country))])
# inequality_select <- sort(unique(data$inequality))[1]



